apiVersion: v1
kind: Service
metadata:
  name: pyroscope
  namespace: monitoring
  labels:
    app: pyroscope
spec:
  ports:
    - name: http
      port: 4040
      targetPort: http
  selector:
    app: pyroscope
