apiVersion: v1
kind: Service
metadata:
  name: prometheus-operator
  namespace: monitoring
  labels:
    app: prometheus-operator
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: prometheus-operator
