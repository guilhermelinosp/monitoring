apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: alloy
  namespace: monitoring
  labels:
    app: prometheus
spec:
  endpoints:
    - port: metrics
  selector:
    matchLabels:
      app: alloy
