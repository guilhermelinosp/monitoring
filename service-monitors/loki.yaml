apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: loki
  namespace: monitoring
  labels:
    app: prometheus
spec:
  endpoints:
    - port: http
  selector:
    matchLabels:
      app: loki
